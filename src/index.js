import React from 'react';
import ReactDOM from 'react-dom';
import Start from './demoComponents/demo1/index'
ReactDOM.render(
  <React.StrictMode>
    <Start/>
  </React.StrictMode>,
  document.getElementById('root')
);
