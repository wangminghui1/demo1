import React from 'react';
import ReactDOM from 'react-dom';
import Start from './demoComponents/resize/index'
ReactDOM.render(
  <React.StrictMode>
    <Start/>
  </React.StrictMode>,
  document.getElementById('root')
);
